---
classifier:
  name: 'BaselineClassifier'
preprocessing:
  params:
    max_brightness: 255
    resize_value: 128
  unification_steps:
    - center_crop
    - resize
  processing_steps:
    - normalize
    - flatten
encode_labels: true